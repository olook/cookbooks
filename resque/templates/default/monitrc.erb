check process <%= @app_name %>_resque
  with pidfile <%= @pidfile %>
  start program = "service <%= @app_name %>_resque start" with timeout 90 seconds
  stop program  = "service <%= @app_name %>_resque stop"  with timeout 90 seconds
  #if totalmem is greater than <%= @max_mem || "300 MB" %> for 10 cycles then restart # eating up memory?
  group resque
