check process <%= @app_name %>_resque with pidfile <%= @pidfile %>
  start program = "/usr/sbin/service <%= @app_name %>_resque start" with timeout 90 seconds
  stop program  = "/usr/sbin/service <%= @app_name %>_resque stop"  with timeout 90 seconds
